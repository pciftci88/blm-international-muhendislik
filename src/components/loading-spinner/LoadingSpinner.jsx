import React from 'react';
import { Container, Loader } from './LoadingSpinner.styles';

function LoadingSpinner() {
    return (
        <Container>
            <Loader />
        </Container>
    );
};

export default LoadingSpinner;